# ボンバーマンterm 仕様書

## 概要
ターミナルで動作するボンバーマン風アクションゲーム。操作感・見た目のテイストは本家に寄せるが、グラフィック相当のcell表現・SE・BGMはすべてオリジナル素材とする（本家の音源・画像は一切使用しない）。

## 技術スタック
- 言語: Rust (stable, cargo)
- TUI描画/入力: ratatui + crossterm
- 音声再生: rodio
- 音声素材: オリジナルのチップチューン風SE/BGM（矩形波・三角波・ノイズ合成のPCMを自前生成し `assets/audio/` にwavとして同梱）
- 乱数: rand

## スコープ (v1 MVP)
- 1人プレイ + CPU敵
- ネットワーク対戦は将来拡張として見据え、入力/プレイヤー処理を抽象化した設計にしておくが、v1では実装しない（ローカル分割キー対戦もv1では作らない）

## ゲームシステム
- マップ: グリッド（例 13×11セル）。外周は破壊不可の壁、内部は格子状に破壊不可ブロックを固定配置、残りマスの一部にランダムで破壊可能ブロックを配置
- 操作: 矢印キー / hjkl で移動、スペースでボム設置、Escでポーズ/終了
- ボム: 設置後一定時間（既定2.5秒）で爆発。爆風は上下左右に既定1マス伸び、アイテムで延長可能。破壊不可ブロックで止まる。破壊可能ブロックは爆風で消滅しアイテムが出現することがある
- 誘爆: 爆風に他のボムが巻き込まれると連鎖的に早期爆発
- 当たり判定: プレイヤー・CPU敵が爆風マスに重なると死亡
- アイテム: 火力アップ・ボム設置数アップ・移動速度アップ・無敵モード（一定時間、爆風・敵接触で死亡しない。無敵中に敵へ触れると敵を撃破する）（キック/貫通等はv2検討）
- CPU敵: 数種類の強さ（ランダム徘徊 / プレイヤー追尾 / 回避寄り）
- ステージクリア条件: 敵を全滅させる。制限時間切れ or プレイヤー死亡でゲームオーバー
- 画面: タイトル → ステージ → クリア/ゲームオーバー → リザルト
- スコア・残機制

## 音声仕様
- BGM（ループ再生）: タイトル、ステージ、クリア、ゲームオーバー
- SE（単発、重ね鳴き対応）: ボム設置、爆発、アイテム取得、被弾/死亡、ステージクリア
- rodioでBGMチャンネルとSEチャンネルを分離管理（BGMは常に1本、SEは複数同時再生可）

## アーキテクチャ
```
src/
  main.rs        エントリポイント、ターミナル初期化(raw mode)、メインループ
  game/          ゲーム状態・エンティティ(Player, Enemy, Bomb, Explosion, Item)・マップ
  render/        ratatui描画
  audio/         rodio再生管理(BGM/SE)
  input/         キー入力→アクション変換（将来ネットワーク対戦時に入力ソースを差し替えられるようtrait化）
assets/
  audio/         オリジナル音源(wav)
```
- メインループは固定tick（例 30fps相当）で 入力処理 → 状態更新 → 描画 の順に回す

## v1では作らないもの（v2で着手。当初の想定から変更した点は明記）
- ローカル分割キー対戦
- アイテム種追加（キック、リモコン爆弾等）、複数マップ、マップエディタ
- ネットワーク対戦は当初「LAN限定のUDPマルチキャスト+ロックステップ」を想定していたが、v2では下記の通りサーバー権威モデルのTCPクライアント/サーバー方式に変更する（LAN限定にせず、ホストのアドレスを指定すればインターネット越しでも接続できる汎用性を優先）

## v2: 4人対戦・ネットワーク対戦・高解像度描画

### 4人対戦（マルチプレイヤー）
- `GameState.player: Player`（単一）を `players: Vec<Player>`（最大4人）に拡張する
- 1人プレイ+CPU敵モードは後方互換として維持（`players.len() == 1` かつ `enemies` が非空のケース）。マルチプレイヤーモードでは `enemies` は空（対人戦なのでCPU敵は出さない）
- 各プレイヤーは色分け・出現位置（マップ四隅に近い位置）を持つ
- 誰か1人が最後まで生き残ったら勝者確定・全滅なら引き分け、というマルチプレイ用の決着判定を追加する（既存の「敵全滅でクリア」とは別ルート）

### ネットワーク対戦（サーバー権威モデル）
- アーキテクチャ: 1人がホスト（サーバー）を兼ね、残り最大3人がクライアントとして接続するリスンサーバー方式。ロックステップ同期（全員の入力を集めて同一ロジックを同時実行）ではなく、**サーバーだけがGameStateを保持・更新し、クライアントは入力を送るだけの薄いクライアント**にする。乱数・浮動小数点の同期ズレを気にしなくてよく、4人・小規模グリッド程度の通信量なら十分実用的なため
- 通信: `tokio` の非同期TCP。UDPではなくTCPを選ぶ理由は、4人対戦程度の通信量なら信頼性(パケットロス対応)を自前実装するコストの方が高くつくため
- シリアライズ: `serde` + `serde_json`（可読性・デバッグのしやすさを優先。パフォーマンスが問題になれば`bincode`等へ差し替え可能な設計にする）
- フレーミング: メッセージ長(4byte)+JSON本体、のシンプルな長さプレフィックス方式
- プロトコル（`src/net/protocol.rs`、実装済みのもの）:
  - `ClientMessage::Input(Action)` — 自分の入力。入力が無いtickは送らない（サーバーは届いた入力だけを次のtickで消費する）
  - `ServerMessage::Welcome { player_id }` — 接続直後に割り当てられるID。0はホストのローカルプレイヤー用に予約し、クライアントは1から採番
  - `ServerMessage::Snapshot(Box<GameState>)` — 毎tickブロードキャストされる最新状態
  - `ServerMessage::PlayerLeft { player_id }` — 切断の通知
  - 当初案から変更した点: 描画用の別型`GameStateSnapshot`は作らず`GameState`をまるごと送る（同じ状態を二重に定義・保守しなくてよいため）。`ClientMessage::Ready`と`ServerMessage::PlayerJoined`は作っていない（開始判断はホストのみ、参加人数はスナップショットの`Screen::Lobby`で全員に届くため不要）
- スレッド構成: **tokioはネットワーク専用スレッドの中だけで動かし、メインループ(crossterm+ratatui)は同期のまま**にする。両者の橋渡しは`std::sync::mpsc`で行う。ゲームロジック(`GameState::tick_multi`)を呼ぶのはホストのメインスレッドで、`net::server`は入力の集約とスナップショットの中継に徹する（既存のメインループを非同期化せずに済ませるため）
- 起動モード（CLI引数、`clap`使用）:
  - `bombermanterm` … 従来通りローカル1人+CPU
  - `bombermanterm host --port <PORT> [--players <N>]` … ホスト(サーバー兼プレイヤー)として起動、他プレイヤーの接続を待つ。`N`は2〜4(既定3)
  - `bombermanterm join <ADDR>` … クライアントとして接続。`ADDR`は`host:port`形式（当初案の`join <ADDR> --port <PORT>`から変更）
- ロビー画面: 参加人数を表示し、必要人数が揃った状態でホストがSPACEを押すとゲーム開始（新規`Screen::Lobby { connected, required }`）。クライアントはこの画面もスナップショットとして受け取って描画する
- 切断耐性: v2の初期実装では「切断されたプレイヤーはその場でリタイア扱い(以降の入力は無し)」に留め、再接続は将来拡張

### 高解像度描画（ドット絵風・ハーフブロック疑似2倍解像度）
- `aquaterm`（`src/framebuffer.rs`）の手法を踏襲: 論理ピクセルグリッド(1マス=8x8ピクセル程度)を持ち、上下2ピクセルを1端末セルにまとめて上半分ブロック文字`▀`(fg=上ピクセル色, bg=下ピクセル色)で描画する。端末フォントは縦長なため、この方式で見た目がほぼ正方形ピクセルになる
- `aquaterm`はratatuiを使わずcrosstermで自前描画しているが、本プロジェクトは既存のratatui資産（タイトル/ステータスバー/リザルト画面のLayoutやParagraph）を活かすため、**ratatuiは維持しつつ、フィールド描画部分だけをピクセルキャンバス方式のカスタムWidgetに置き換える**（`render/pixel_canvas.rs`を新設）
- 各要素（プレイヤー4色・敵3種・壁・ブロック・ボム・爆風・アイテム4種）はパレットインデックスの8x8(または16x16)ドット絵スプライトとして定義し、本家ボンバーマンを彷彿とさせるシルエット（プレイヤー=丸い頭に球体スーツ、敵=丸いバルーン風モンスター、壁=陰影付きコンクリートブロック、ブロック=木箱調）に寄せる。ただし配色・輪郭は完全にオリジナルとし、本家の画像は一切参照・使用しない

### 画面サイズ可変
- 端末リサイズへの追従: ratatuiは`terminal.draw()`のたびに現在の端末サイズを使うため、追加実装なしで自動追従する
- ズーム機能（新規）: `+`/`-`キーで表示スケール(1x/2x/3x)を切り替え、1論理ピクセルを何文字×何文字で表現するかを変える。ピクセルキャンバスを最近傍拡大（nearest neighbor）してからハーフブロック変換する

### v2の技術スタック追加分
- `tokio`（非同期ランタイム、TCP通信）
- `serde` + `serde_json`（ネットワークメッセージのシリアライズ）
- `clap`（CLI引数パース: host/join/ポート指定）

### v2アーキテクチャ追加分
```
src/
  net/
    mod.rs       NetRole(Local/Host/Client)の切り替えとゲームループへの配線
    protocol.rs  ClientMessage/ServerMessage定義
    server.rs    tokio TCPリスナー、複数クライアント管理、tick駆動でGameState更新+ブロードキャスト
    client.rs    tokio TCP接続、入力送信、Snapshot受信
  render/
    pixel_canvas.rs  論理ピクセルグリッド+ハーフブロック変換+スプライト定義
```

### v2実装フェーズ（順次進める）
1. `GameState`をN人プレイヤー対応にリファクタリング（ローカルのみ、ネットワークはまだ繋がない）
2. 高解像度ドット絵描画への刷新（ピクセルキャンバス+スプライト+ズーム）
3. ネットワーク層実装（サーバー/クライアント/ロビー画面）
4. 統合・調整（4人揃っての実プレイ確認、ローカル2プロセスでの疎通確認）

## テスト方針
- ゲームロジック（爆風範囲計算、当たり判定、アイテム効果、誘爆連鎖、複数プレイヤーの決着判定）はユニットテストでカバー
- ネットワークプロトコルのシリアライズ/デシリアライズ往復はユニットテストでカバー。実際の接続確認は同一マシン上で複数プロセスを起動して手動確認
- 描画・音声は `cargo run` による実プレイで手動確認
